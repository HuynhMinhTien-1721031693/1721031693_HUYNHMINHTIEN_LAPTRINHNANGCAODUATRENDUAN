# Core Assets

Create these first:

- `ST_ScanObject` based on `FScanObjectRow`
- `E_ScanState` based on `EScanState`
- `DA_ScannerConfig` based on `FScannerConfig` defaults

Keep state definitions centralized here so actors and UI read one source.
