# Maps Folder

Create `L_CameraScan_Test` here for week-1 validation.

Place in map:

- `BP_StateManager`
- `BP_MockDataManager`
- `BP_CameraScanner`
- `BP_ConveyorBelt`
- PlayerStart + HUD bootstrap entry
